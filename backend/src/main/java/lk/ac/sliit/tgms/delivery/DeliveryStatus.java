package lk.ac.sliit.tgms.delivery;

/** Controlled Delivery Management lifecycle values for the current project scope. */
public enum DeliveryStatus {
    SCHEDULED,
    OUT_FOR_DELIVERY,
    DELIVERED,
    CANCELLED
}
